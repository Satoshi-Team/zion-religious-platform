import { LucideProps } from "lucide-react"

interface DoveIconProps {
  className?: string
}

export function DoveIcon({ className }: DoveIconProps) {
  return (
    <span className={className}>
      ��️
    </span>
  )
} 